---
import { Image } from "astro:assets"
import CTreffLogo from "../assets/ctreffos-logo.png"
import { getLangFromUrl, useTranslations } from "../i18n/utils"
import { Navigation as Nav } from "@/components/Navigation.tsx"

const lang = getLangFromUrl(Astro.url)
const t = useTranslations(lang)
---

<div class="relative z-1">
  {
    lang === "de" && (
      <a
        class="absolute top-0 right-0 p-4"
        href={`/en`}
        title="Switch to English"
      >
        🇬🇧
      </a>
    )
  }
  {
    lang === "en" && (
      <a
        class="absolute top-0 right-0 p-4"
        href={`/de`}
        title="Auf Deutsch wechseln"
      >
        🇩🇪
      </a>
    )
  }
</div>
<a href={`/${lang}`} title={t("nav.home")}>
  <header class="flex flex-col items-center">
    <Image
      src={CTreffLogo}
      alt="Chaostreff Osnabrück Logo"
      class="invert-0 dark:invert"
      width={517}
      height={299}
      loading="eager"
    />
  </header>
</a>

<Nav lang={lang} />
